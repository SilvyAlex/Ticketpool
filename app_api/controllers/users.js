//Controllers
/*const mongoose = require ('mongoose'); //incorporar mongoose a la rest api
const users = mongoose.model ('user');//El modelo me permite interactuar

//Crear ususraio
const userCreate = (req, res) =>{ 
    res
        .status(201)
        .json({"status":"sucess create"});
};


//Modificar usuarios
const userList = (req, res) =>{ 
    res
        .status(200)
        .json({"status":"sucess list"});

};

//Leer usurio
const userRead = (req, res) =>{ 
    res
        .status(200)
        .json({"status":"sucess read"});

};

//Modificar usuarios
const userUpdate = (req, res) =>{ 
    res
        .status(200)
        .json({"status":"sucess read"});

};

//Eliminar usuarios
const userDelete = (req, res) =>{ 
    res
        .status(204)
        .json({"status":"sucess delete"});
};

module.exports = {
    userCreate,
    userList,
    userRead,
    userUpdate,
    userDelete,
    
}*/